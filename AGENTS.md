# AGENTS.md

给在本项目中工作的 AI 助手和新协作者的说明。动手前请先读完本文件和 [README.md](README.md)。

## 项目背景

《软件工程》课程设计：电子相册系统。当前处于**需求分析阶段**，交付物是面向对象需求分析报告（按老师给定模板）+ 答辩 PPT，后续还有面向对象设计报告和系统实现（Vue + FastAPI + PostgreSQL）。

题目（老师给定的用例）固定了功能范围：注册、登录、创建相册、修改相册、上传图片、浏览照片、评论照片、管理员维护相册类别。**不要新增功能**（如视频、点赞、关注、好友机制）——超出题目不加分，只会增加画图和答辩工作量。

## 已定决策（改动需组长确认）

1. **范围约定**："分享" = 所有登录用户可浏览他人公开相册，无好友机制；相册分公开/私有；用户可删除自己的评论（作为评论用例的扩展流程）；管理员登录复用用户登录。
2. **技术栈**：Vue 3 + TS + Vite / FastAPI / PostgreSQL（Docker 容器）。ORM 倾向 SQLAlchemy，认证倾向 JWT，均待设计阶段定稿。
3. **照片存储**：开发期照片存本机 `uploads/` 目录（文件系统），数据库只存路径与元数据；后期可演进为独立文件服务器/对象存储（报告图 2-1 已按此前期/后期两阶段表达）。
4. **运行环境**：开发期全系统跑在开发本机，"服务器"指本机上运行的 FastAPI 进程，不是云服务器。
5. **数据同步**：`seed.sql` + `demo_photos/` 进 Git；每人本地 Docker 数据库执行 seed 脚本；`uploads/` 与数据库数据文件不进 Git。

## 图稿工作方式（重要）

- 报告图稿由 `tools/` 下的 Python 脚本以 XML 方式生成 `.drawio` 文件，再经 draw.io 命令行导出 PNG：
  `drawio -x -f png --scale 2 -o 输出.png 输入.drawio`
  （`drawio` 指**各自本机**安装的 draw.io 可执行文件，各人路径不同，不要把本机绝对路径写进任何项目文件。）
- **改图流程：改脚本 → 重新生成 .drawio → 导出 PNG → 用图像查看工具自检 → 交付**。不要直接手改 .drawio XML。
- 参考脚本：`tools/gen_drawio.py`（组长：图 2-1、2-2）、`tools/gen_login_demo.py`（成员用例图/活动图/类模型图/协作图的写法示例）。组员在各自本机让自己的 AI 助手参照它编写自己模块的画图脚本。
- 所有图必须遵守 [docs/绘图规范.md](docs/绘图规范.md)（黑白风格、字体、走线不穿框、导出设置等）。
- 项目内所有路径一律用相对项目根目录（`Electronic Photo Album System/`）的形式书写，不写任何本机绝对路径——仓库要在 GitHub 上协作，各人的本机路径不一样。
- 图编号规则：第 1 章系统目标、第 2 章功能需求。图 2-1 = 系统体系结构图，图 2-2 = 功能框架图，之后按报告出现顺序连续编号，整合排版时统一核对（成员交付的图文件名不带编号）。

## 报告要求红线（来自老师模板）

- 每幅图必须有编号和图名，且图后必须有对应的文字说明（不允许只贴图）。
- 用例描述要写清楚（参与者、前置条件、基本流程、扩展流程、后置条件）。
- 功能描述中活动图与泳道图二选一（本组统一用活动图，除非某功能确有多角色交互）。
- 逻辑分析与建模中每个功能要有类模型图 + 协作图。
- 报告总页数不低于 10 页；排版质量计入评分。

## 当前任务状态

1. [x] 需求梳理、系统目标、功能模块划分 → `docs/需求分析/01_*.md`
2. [x] 用例描述初稿（8 用例）→ `docs/需求分析/02_*.md`
3. [x] 技术选型、绘图规范 → `docs/绘图规范.md`
4. [x] 图 2-1 系统体系结构图、图 2-2 功能框架图 → `docs/图稿/`
5. [~] 阶段 2+3：各成员负责用例的功能描述（用例图 + 活动图）与逻辑分析（类模型图 + 协作图）→ `docs/交付/编号-模块名-姓名/`
   - 已完成交付演示：成员B「用户登录」全套（`docs/交付/1-账户管理-成员B/用户登录.md`），作为全组模板
6. [ ] 阶段 4：非功能需求、任务分工、总结、整合排版（页数 ≥ 10）
7. [ ] 阶段 5：答辩 PPT
8. [ ] 系统实现与期末交付（源码 + SQL 脚本）

## 分工（5 人组，每人两个功能用例）

- 成员 A（组长）：统筹与质量把关；已完成系统目标、图 2-1、图 2-2；负责"系统结构"节图后说明、图编号统一、整合排版、非功能需求章节初稿
- 成员 B：用户注册、用户登录（账户管理）
- 成员 C：创建相册、修改相册（相册管理·用户侧）
- 成员 D：上传图片、浏览照片（照片管理）
- 成员 E：相册类别管理（管理员）、对照片评论（评论管理）

每个用例的交付内容：**功能描述**（六段式用例描述 + 模块用例图 + 活动图）+ **逻辑分析与建模**（类模型图 + 协作图），每幅图配文字说明。用例图按模块共用一张。

## 交付与协作（GitHub）

- 组长把本仓库推到 GitHub（私有），组员为 collaborator；每人建分支 `成员X-模块名`，完成后发 PR，组长 review 后合并。
- 交付物放 `docs/交付/编号-模块名-姓名/`（如 `1-账户管理-张三`）：**一人一个文件夹，只动自己的文件夹**；一个用例一个 md，图片放 `images/`（PNG + `.drawio` 源文件一起提交），md 里用相对路径引用图片。
- 公共文件（本文件、`README.md`、`docs/需求分析/`、`docs/绘图规范.md`、`tools/` 共享脚本、`docs/交付/README.md`）由组长统一维护。
- 详细交付规范和提交前自查清单见 [docs/交付/README.md](docs/交付/README.md)。

图稿初稿由 AI 生成，组员负责校对、撰写图后说明文字和自查清单核对。
