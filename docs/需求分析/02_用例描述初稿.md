# 电子相册系统 · 阶段 2 草稿：用例描述 + 画图说明

> 每个用例按六段式写：用例名称、参与者、前置条件、基本流程、扩展流程、后置条件。
> 画图对照规则：参与者=小人；基本流程和扩展流程=活动图的步骤和判断分支；
> 用例里的名词→后面类图的候选类，动词→候选方法。

## 模块一：账户管理

### 用例 1：用户注册
- **参与者**：访客（未注册用户）
- **前置条件**：无
- **基本流程**：
  1. 用户打开注册页面；
  2. 输入用户名、密码、确认密码、邮箱；
  3. 系统检查用户名是否已被注册；
  4. 用户名可用，系统创建新账户；
  5. 提示注册成功，自动跳转登录页面。
- **扩展流程**：
  - 2a. 两次输入密码不一致，系统提示错误，返回步骤 2；
  - 2b. 输入信息不完整（必填项为空），系统提示补全，返回步骤 2；
  - 3a. 用户名已存在，系统提示"该用户名已被占用"，返回步骤 2。
- **后置条件**：系统中新增一个普通用户账户。

### 用例 2：用户登录
- **参与者**：普通用户、管理员
- **前置条件**：用户已注册（账户已存在）
- **基本流程**：
  1. 用户打开登录页面；
  2. 输入用户名和密码；
  3. 系统验证用户名和密码；
  4. 验证通过，系统根据账户角色跳转：普通用户进入个人主页，管理员进入后台管理页。
- **扩展流程**：
  - 3a. 用户名不存在或密码错误，系统提示"用户名或密码错误"，返回步骤 2；
  - 2a. 用户名或密码为空，系统提示补全，返回步骤 2。
- **后置条件**：用户处于登录状态，系统记录当前登录用户及其角色。
- **说明**：管理员登录复用本用例，不单独画。

## 模块二：相册管理

### 用例 3：创建相册
- **参与者**：普通用户
- **前置条件**：用户已登录
- **基本流程**：
  1. 用户进入"我的相册"页面，点击"创建相册"；
  2. 系统显示创建表单：相册名称、相册类别（系统类别列表）、公开/私有设置；
  3. 用户填写信息并提交；
  4. 系统校验（名称非空、类别已选）；
  5. 系统创建相册，并关联到当前用户；
  6. 提示创建成功，返回相册列表，新相册出现在列表中。
- **扩展流程**：
  - 4a. 相册名称为空或未选类别，系统提示错误，返回步骤 3。
- **后置条件**：系统中新增一个属于当前用户的相册。

### 用例 4：修改相册
- **参与者**：普通用户（相册所有者）
- **前置条件**：用户已登录，相册已存在
- **基本流程**：
  1. 用户在相册列表中选择相册，点击"修改"；
  2. 系统显示该相册的现有信息；
  3. 用户修改相册名称、类别或公开/私有属性并提交；
  4. 系统校验；
  5. 系统更新相册信息；
  6. 提示修改成功，返回相册列表。
- **扩展流程**：
  - 4a. 名称为空，系统提示错误，返回步骤 3；
  - 1a. 相册不存在或已被删除，系统提示，返回相册列表。
- **后置条件**：相册信息被更新（如私有改公开后，其他用户可浏览）。

### 用例 5：相册类别管理
- **参与者**：管理员
- **前置条件**：管理员已登录（后台）
- **基本流程**：
  1. 管理员进入后台"相册类别管理"页面；
  2. 系统显示现有类别列表；
  3. 管理员执行添加、修改或删除类别操作并提交；
  4. 系统更新类别字典；
  5. 提示操作成功，刷新类别列表。
- **扩展流程**：
  - 4a. 删除类别时，若该类别下仍存在相册，系统提示"该类别下存在相册，不能删除"，返回步骤 2；
  - 3a. 新类别名称为空或与已有类别重名，系统提示错误。
- **后置条件**：类别字典更新，"创建相册"的可选类别随之变化。

## 模块三：照片管理

### 用例 6：上传图片
- **参与者**：普通用户
- **前置条件**：用户已登录，目标相册已存在
- **基本流程**：
  1. 用户进入目标相册，点击"上传图片"；
  2. 系统显示文件选择窗口；
  3. 用户选择本地图片文件并提交；
  4. 系统校验文件格式（jpg/png/gif）和大小（不超过 10MB）；
  5. 校验通过，系统保存图片文件并生成缩略图，记录上传者和上传时间；
  6. 提示上传成功，照片显示在相册中。
- **扩展流程**：
  - 4a. 格式或大小不符合要求，系统提示"仅支持 jpg/png/gif，且不超过 10MB"，返回步骤 3。
- **后置条件**：相册中新增一张照片（记录上传者、所属相册、上传时间）。

### 用例 7：浏览照片
- **参与者**：普通用户
- **前置条件**：用户已登录
- **基本流程**：
  1. 用户进入浏览页面，系统显示所有公开相册列表（含本人的全部相册）；
  2. 用户点击某个相册；
  3. 系统显示该相册内照片的缩略图网格；
  4. 用户点击某张缩略图；
  5. 系统显示照片大图及照片信息（拍摄/上传时间、上传者、评论列表）。
- **扩展流程**：
  - 1a. 不存在任何可见相册，系统显示"暂无相册"；
  - 2a/链接直达：若用户访问他人私有相册，系统提示"该相册为私有，无权查看"，返回步骤 1。
- **后置条件**：无（只读操作，不改变系统状态）。

## 模块四：评论管理

### 用例 8：对照片评论
- **参与者**：普通用户
- **前置条件**：用户已登录，照片对其可见（公开相册中的照片，或本人相册中的照片）
- **基本流程**：
  1. 用户在照片大图页输入评论内容，点击"发表"；
  2. 系统校验评论内容（非空，不超过 200 字）；
  3. 系统保存评论，关联评论用户与照片，并记录评论时间；
  4. 评论显示在照片下方的评论列表中（按时间倒序）。
- **扩展流程**：
  - 2a. 内容为空或超长，系统提示错误；
  - 4a. 用户对自己发表的评论点击"删除"，系统确认后移除该评论。
- **后置条件**：照片新增一条评论记录（删除时相应移除）。

---

## 附：画图要点提醒（阶段 2 交付）

**用例图**：小人=参与者，椭圆=用例，大框=系统边界。例：注册/登录/创建相册等属于"普通用户"；相册类别管理属于"管理员"；登录可被两个小人连线（include/泛化说明管理员复用）。

**活动图**：基本流程按顺序画步骤；扩展流程画成分支（菱形判断）。例：注册活动图在"检查用户名"后画菱形"用户名已存在？"，是→提示→回到输入，否→创建账户→结束。

**注意**：每幅图要有编号和图名（如"图 3-1 用户注册用例图"），图后必须有文字说明，不能光贴图。
